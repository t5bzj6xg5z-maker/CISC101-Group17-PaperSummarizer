Module 01 — Intake & Setup

## Change Log
- v1.0

## Purpose
Normalize inputs, detect flags, prep chunking.

## Inputs
- Raw paper text; optional ordered section list.

## Outputs
- `sections[]`: {name, text, word_count, flags ∈ {missing, empty, short}}
- `chunks[]` (optional), `issues[]`

## Steps
1) Normalize section list (infer if absent).
2) Split by headings/heuristics; compute `word_count`.
3) Flags: `missing` (listed but absent), `empty` (len 0), `short` (<50 words).
4) Create `chunks[]` for long sections; record `issues[]`.

